import styled from 'styled-components';

export const WeekdayLabel = styled.div<{ isWeekend: boolean }>`
  padding: 6px;
  text-align: center;
  font-weight: bold;
  font-size: 14px;
  font-weight: 700;
`;
